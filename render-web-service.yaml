services:
  - type: web
    name: portfolio-backend
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: NODE_ENV
        value: production
    # Health check configuration
    healthCheckPath: /health
